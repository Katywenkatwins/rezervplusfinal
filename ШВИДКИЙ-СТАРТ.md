# 🚀 Швидкий старт - Деплой на GitHub Pages

## ✅ Що було виправлено

- Переміщено `deploy.yml` в правильну папку `/.github/workflows/`
- Налаштовано `vite.config.ts` для копіювання файлів з `public/`
- Виправлено `manifest.json` для правильної роботи PWA
- Додано адаптивну ширину та висоту на весь viewport
- Зафіксовано навігаційне меню внизу
- Додано приховування скролбара

## 📋 Команди для деплою

```bash
# 1. Додати всі зміни
git add -A

# 2. Закомітити
git commit -m "Fix: виправлено структуру проекту для GitHub Pages"

# 3. Відправити на GitHub
git push origin main
```

## 🔍 Перевірка деплою

1. Відкрийте https://github.com/YOUR_USERNAME/Rezerv/actions
2. Зачекайте поки workflow завершиться (1-2 хвилини)
3. Відкрийте https://katyewenkatwins.github.io/Rezerv/

## ⚙️ Налаштування GitHub (якщо потрібно)

### Увімкнути GitHub Pages

1. Відкрийте репозиторій на GitHub
2. Settings → Pages
3. Source: **GitHub Actions** (має бути вибрано автоматично)

### Права доступу для Actions

1. Settings → Actions → General
2. Workflow permissions: **Read and write permissions** ✓
3. Натисніть Save

## 📱 PWA Іконки (опціонально)

Для генерації PNG іконок:

1. Відкрийте `/public/generate-icons.html` в браузері
2. Натисніть "Завантажити всі іконки"
3. Помістіть згеновані файли в `/public/`
4. Оновіть `manifest.json` (або використайте `manifest-with-icons.json`)
5. Закомітьте та push

## 🎯 Результат

Після успішного деплою ваш додаток:
- ✅ Працює на GitHub Pages
- ✅ Адаптивний (працює на всіх розмірах екранів)
- ✅ Має фіксоване меню внизу
- ✅ Контент скролиться без видимої полоси
- ✅ Готовий як PWA (можна встановити на телефон)

## 🐛 Що робити якщо не працює?

Перегляньте файл `/DEPLOY-FIX.md` для детального troubleshooting.
