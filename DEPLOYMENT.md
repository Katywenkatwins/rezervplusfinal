# 🚀 Інструкція з деплою на GitHub Pages

## ⚠️ ВАЖЛИВО: Структура проєкту

**НЕ ПОТРІБНО** переміщувати файли з папки `src` в корінь репозиторію!

Ваш проєкт використовує **Vite** (збірщик для React). Структура така:
- **Вихідні файли** (код) → в папках `/src`, `/components`, `/public` тощо
- **Зібрані файли** (для деплою) → автоматично створюються в папці `/dist` при збірці
- **GitHub Actions** автоматично збирає проєкт і деплоїть тільки папку `/dist`

Vite автоматично копіює всі файли з `/public` в `/dist` при збірці. Тому:
- `manifest.json` → з `/public` автоматично потрапить в `/dist`
- `service-worker.js` → з `/public` автоматично потрапить в `/dist`
- Іконки PNG → з `/public` автоматично потраплять в `/dist`
- `app.js` → з `/public` автоматично потрапить в `/dist`

**Шляхи в `manifest.json` та інших файлах вже налаштовані правильно** з урахуванням base path `/Rezerv/`.

---

## Крок 1: Підготовка іконок

### Варіант A: Автоматична генерація (швидко)
1. Відкрийте файл `/public/generate-icons.html` в браузері
2. Натисніть "Завантажити всі іконки"
3. Збережіть всі PNG файли в папку `/public`

### Варіант B: Онлайн генератор (краща якість)
1. Перейдіть на https://www.pwabuilder.com/imageGenerator
2. Завантажте ваш логотип (512x512px або більше)
3. Завантажте згенеровані іконки
4. Помістіть файли в `/public`:
   - `icon-128x128.png`
   - `icon-152x152.png`
   - `icon-180x180.png`
   - `icon-192x192.png`
   - `icon-512x512.png`

## Крок 2: Налаштування GitHub Repository

1. **Перевірте назву репозиторію**
   - Має бути **Rezerv** (з великої літери!)
   - Якщо назва інша - змініть `base` в `vite.config.ts`

2. **Налаштуйте GitHub Pages**
   - Перейдіть в **Settings** → **Pages**
   - **Source**: оберіть **GitHub Actions**
   - Збережіть зміни

3. **Створіть папку `.github/workflows`** (якщо не існує)
   - Перенесіть файл `/workflows/deploy.yml` в `.github/workflows/deploy.yml`
   - Структура має бути: `.github/workflows/deploy.yml`

## Крок 3: Деплой

1. **Перевірте що всі файли на місці:**
   ```
   /public/manifest.json          ✅
   /public/service-worker.js      ✅
   /public/app.js                 ✅
   /public/icon-placeholder.svg   ✅
   /public/icon-128x128.png       📝 створіть!
   /public/icon-152x152.png       📝 створіть!
   /public/icon-180x180.png       📝 створіть!
   /public/icon-192x192.png       📝 створіть!
   /public/icon-512x512.png       📝 створіть!
   .github/workflows/deploy.yml   ✅
   ```

2. **Закомітьте всі зміни:**
   ```bash
   git add .
   git commit -m "Fix PWA paths for GitHub Pages deployment"
   git push origin main
   ```

3. **Перевірте GitHub Actions:**
   - Перейдіть на вкладку **Actions** в репозиторії
   - Зачекайте поки workflow завершиться (зелена галочка ✅)
   - Зазвичай займає 2-3 хвилини

4. **Відкрийте ваш сайт:**
   - https://katywenkatwins.github.io/Rezerv/

## Крок 4: Перевірка PWA

1. **Очистіть кеш браузера:**
   - Натисніть `Ctrl+Shift+Delete` (Windows/Linux)
   - Або `Cmd+Shift+Delete` (Mac)
   - Оберіть "Зображення та файли в кеші"
   - Натисніть "Очистити"

2. **Зробіть Hard Refresh:**
   - `Ctrl+Shift+R` (Windows/Linux)
   - `Cmd+Shift+R` (Mac)

3. **Перевірте Manifest:**
   - Відкрийте DevTools (F12)
   - Перейдіть на вкладку **Application**
   - Виберіть **Manifest**
   - Має відображатися без помилок

4. **Перевірте іконки:**
   - Відкрийте: https://katywenkatwins.github.io/Rezerv/manifest.json
   - Має бути валідний JSON з іконками
   - Відкрийте: https://katywenkatwins.github.io/Rezerv/icon-192x192.png
   - Має відображатися іконка

5. **Перевірте Service Worker:**
   - Відкрийте DevTools (F12)
   - Перейдіть на вкладку **Application** → **Service Workers**
   - Має бути зареєстрований Service Worker для `/Rezerv/`
   - Status має бути "activated and is running"

## 🎉 Готово!

Якщо все працює правильно:
- ✅ Сайт доступний за адресою
- ✅ Manifest знайдено без помилок
- ✅ Іконки завантажуються
- ✅ Service Worker активний
- ✅ PWA можна встановити на пристрій

## 🐛 Вирішення проблем

### Manifest 404 (не знайдено)

**Причина:** Файли з `/public` не скопіювалися в `dist`

**Рішення:**
1. Перевірте чи файл `/public/manifest.json` існує на GitHub
2. Перевірте чи GitHub Actions успішно завершився
3. Подивіться логи GitHub Actions - чи є помилки збірки
4. Очистіть кеш та зробіть Hard Refresh

### Іконки 404 (не знайдено)

**Причина:** PNG іконки не створені або не закомічені

**Рішення:**
1. Згенеруйте іконки (Крок 1)
2. Помістіть їх в папку `/public`
3. Закомітьте іконки на GitHub
4. Зачекайте завершення деплою

### Service Worker помилка "bad-precaching-response"

**Причина:** Service Worker намагається закешувати файли, які не існують

**Рішення:**
1. Перевірте що всі файли в `urlsToCache` існують
2. Очистіть Service Worker: DevTools → Application → Service Workers → Unregister
3. Зробіть Hard Refresh

### PWA не встановлюється

**Причина:** Manifest невалідний або іконок бракує

**Рішення:**
1. Відкрийте DevTools → Application → Manifest
2. Перевірте Console на помилки
3. Переконайтеся що всі іконки доступні (особливо 192x192 і 512x512)
4. Перевірте що `start_url` і `scope` правильні

### GitHub Actions не запускається

**Причина:** Workflow файл не в правильному місці

**Рішення:**
1. Файл має бути `.github/workflows/deploy.yml` (з крапкою на початку!)
2. Перевірте чи він закомічений на GitHub
3. Перевірте Settings → Actions → General:
   - Workflow permissions → Read and write permissions
   - Allow GitHub Actions to create and approve pull requests → ✅

### Подвійна структура на GitHub Pages

**Проблема:** Сайт відкривається за адресою `username.github.io/Rezerv/Rezerv/`

**Причина:** Неправильний `base` в `vite.config.ts` або неправильне налаштування GitHub Pages

**Рішення:**
1. Перевірте `vite.config.ts`: `base: '/Rezerv/'` (має співпадати з назвою репозиторію)
2. Перевірте Settings → Pages → Source = "GitHub Actions" (НЕ "Deploy from a branch")
3. Видаліть всі старі деплої: Settings → Pages → видаліть попередні deployment якщо є

## 📝 Важливі примітки

### Чому НЕ можна просто скопіювати файли в корінь?

1. **Vite збирає проєкт** - він компілює React, оптимізує код, мінімізує файли
2. **Vite обробляє імпорти** - перетворює `import` в правильні шляхи
3. **Vite додає хеші до файлів** - для кешування (наприклад, `app.abc123.js`)
4. **Vite копіює статичні файли** - з `/public` в `/dist` автоматично

Якщо просто скопіювати файли з `src` в корінь - додаток не буде працювати!

### Як працює деплой?

1. **GitHub Actions** запускається при push в `main`
2. **npm ci** - встановлює залежності
3. **npm run build** - Vite збирає проєкт в папку `/dist`
4. **actions/upload-pages-artifact** - завантажує `/dist` на GitHub Pages
5. **actions/deploy-pages** - публікує на `username.github.io/Rezerv/`

### Як змінити назву репозиторію?

Якщо хочете щоб сайт був за адресою `username.github.io/my-app/`:

1. Перейменуйте репозиторій на GitHub в "my-app"
2. Змініть `vite.config.ts`: `base: '/my-app/'`
3. Змініть `manifest.json`: `start_url` і `scope` на `/my-app/`
4. Змініть `service-worker.js`: `BASE_PATH` на `/my-app`
5. Змініть `app.js`: шлях service worker на `/my-app/service-worker.js`
6. Закомітьте зміни

## 📞 Додаткова допомога

Якщо проблема не вирішується:
1. Перевірте Console в DevTools на помилки
2. Перевірте Network tab в DevTools - які файли повертають 404
3. Перевірте логи GitHub Actions - чи є помилки збірки
4. Порівняйте з робочим прикладом: https://vitejs.dev/guide/static-deploy.html#github-pages

---

**Дата оновлення:** 11 грудня 2024
