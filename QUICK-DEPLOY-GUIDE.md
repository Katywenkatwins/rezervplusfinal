# ⚡ Швидкий гайд для деплою

## 🎯 Головне правило

**НЕ переміщуйте файли з `/src` в корінь!** Vite автоматично збирає проєкт.

---

## ✅ Чеклист перед деплоєм

### 1. Перевірте файли PWA в `/public`:
```
✅ manifest.json
✅ service-worker.js
✅ app.js
✅ icon-placeholder.svg
📝 icon-128x128.png    (створіть!)
📝 icon-152x152.png    (створіть!)
📝 icon-180x180.png    (створіть!)
📝 icon-192x192.png    (створіть!)
📝 icon-512x512.png    (створіть!)
```

### 2. Перевірте GitHub Actions:
```
✅ .github/workflows/deploy.yml існує
```

Якщо файл знаходиться в `/workflows/deploy.yml`, перенесіть його в `.github/workflows/deploy.yml`

### 3. Перевірте GitHub Settings:
- Settings → Pages → Source = **"GitHub Actions"**

---

## 🚀 Деплой за 3 кроки

### Крок 1: Згенеруйте іконки
Відкрийте `/public/generate-icons.html` в браузері і завантажте всі іконки в `/public`

### Крок 2: Закомітьте зміни
```bash
git add .
git commit -m "Fix PWA paths and add icons"
git push origin main
```

### Крок 3: Зачекайте деплою
Перейдіть на вкладку **Actions** на GitHub і зачекайте зеленої галочки ✅

---

## 🔍 Перевірка

Відкрийте: https://katywenkatwins.github.io/Rezerv/

**DevTools (F12) → Application:**
- Manifest → має відображатися без помилок
- Service Workers → має бути активний
- Іконки → всі мають завантажуватися

---

## 🐛 Якщо щось не працює

### Manifest 404
→ Перевірте чи є `/public/manifest.json` на GitHub

### Іконки 404  
→ Згенеруйте іконки і закомітьте в `/public`

### Service Worker помилка
→ DevTools → Application → Service Workers → Unregister → Hard Refresh

### Подвійний шлях (Rezerv/Rezerv/)
→ Settings → Pages → Source = "GitHub Actions" (не "Deploy from a branch")

---

## 📋 Структура проєкту (як має бути)

```
Rezerv/                          # Корінь репозиторію
├── .github/
│   └── workflows/
│       └── deploy.yml           # ✅ Тут має бути workflow
├── src/                         # Вихідний код React
│   └── main.tsx
├── components/                  # React компоненти
│   └── ...
├── public/                      # Статичні файли (автоматично копіюються в dist)
│   ├── manifest.json            # ✅ PWA manifest
│   ├── service-worker.js        # ✅ Service Worker
│   ├── app.js                   # ✅ Реєстрація SW
│   ├── favicon.svg
│   ├── icon-placeholder.svg
│   └── icon-*.png               # ✅ PNG іконки (створіть!)
├── index.html                   # Головна сторінка
├── vite.config.ts               # Конфігурація Vite
└── package.json

# При збірці створюється:
dist/                            # НЕ комітьте це! Створюється автоматично
├── index.html
├── assets/
│   └── *.js, *.css
├── manifest.json                # Скопійовано з /public
├── service-worker.js            # Скопійовано з /public
├── app.js                       # Скопійовано з /public
└── icon-*.png                   # Скопійовано з /public
```

---

## 💡 Важливо!

- **Всі зміни** робіть в вихідних файлах (`/src`, `/components`, `/public`)
- **НЕ редагуйте** файли в `/dist` (ця папка генерується автоматично)
- **НЕ комітьте** папку `/dist` на GitHub (вона в .gitignore)
- **Vite автоматично** збирає проєкт і створює `/dist` при деплої

---

**Дата:** 11 грудня 2024
